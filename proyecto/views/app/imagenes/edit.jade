extends ../../layout.jade
block contenido
	div(class="col-md-6 remove-float center-block big-top-space")
		h1 Subir Nueva Imagen
		form(action="/app/imagenes/#{imagen.id}?_method=PUT",method="POST")
			div(class="form-group")
				label(for="title") Codigo
				input(type="text", name="title", placeholder="Titulo de la Imagen",id="title",class="form-control",value="#{imagen.title}")
			div 
				label(for="contenido") Contenido
				input(type="text", name="contenido", placeholder="Contenido a cambiar",id="contenido",class="form-control",value="#{imagen.contenido}")			
		
			div(class="row top-space")
				div(class="col-xs-12 col-sm-6")
					input(type="submit" value="Guardar" class="btn btn-info")